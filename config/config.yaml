server:
  port: 8080
  mode: debug
auth:
  jwt_secret: test-secret-key
  jwt_expiry: 24h
llm:
  api_key: test-key
  base_url: http://localhost:9999/v1
  model: gpt-4
logging:
  level: debug
  format: json
